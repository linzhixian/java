package com.funugame.parkour.dao;

import java.util.List;
import java.util.Map;

import org.apache.ibatis.annotations.Param;

import com.funugame.parkour.api.daoentity.UserProp;

public interface UserPropMapper extends BaseMapper<UserProp> {
    public List<UserProp> selectList(int uid);
    List<UserProp> selectPropList(UserProp up);
    public List<UserProp> selectGamePropList(UserProp prop);
    int insert(UserProp up);
    int update(UserProp up);
    int delete(UserProp up);
}
