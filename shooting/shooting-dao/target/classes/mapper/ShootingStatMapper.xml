<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.funugame.parkour.dao.UserAchievementMapper">
	<cache eviction="LRU" flushInterval="86400000" size="2048" />


	<insert id="insert" parameterType="ShootingStat">
		INSERT INTO
		shooting_stat(uid,cid,sdate,wincount,failcount)
		value(#{uid},#{cid},#{sdate},#{wincount},#{failcount});

	</insert>

	<select id="selectOne" parameterType="ShootingStat" resultType="ShootingStat">
		select *
		from shooting_stat where uid=#{uid} and cid=#{cid} and sdate=#{sdate};
	</select>


	<update id="addWinCount" parameterType="hashmap">
		UPDATE shooting_stat set 
		wincount=wincount+1
		WHERE uid = #{uid} and cid=#{cid} and sdate=#{sdate}
	</update>

	<update id="addFailCount" parameterType="hashmap">
		UPDATE shooting_stat set 
		failcount=failcount+1
		WHERE uid = #{uid} and cid=#{cid} and sdate=#{sdate}
	</update>
</mapper>